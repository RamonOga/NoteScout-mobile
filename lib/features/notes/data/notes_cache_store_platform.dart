/// Выбор хранилища снимка под платформу.
///
/// Условный импорт здесь не украшение: реализация для телефона использует
/// `dart:io`, которого в сборке для web не существует. Без этой развилки
/// веб-сборка перестала бы компилироваться целиком — вместе с предпросмотром
/// интерфейса в браузере.
library;

export 'notes_cache_store_noop.dart'
    if (dart.library.io) 'notes_cache_store_io.dart';
